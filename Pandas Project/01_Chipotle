import pandas as pd
import numpy as np
chipo = pd.read_csv(
    "https://raw.githubusercontent.com/justmarkham/DAT8/master/data/chipotle.tsv", on_bad_lines="skip")
print(chipo.head(10))
print(chipo.shape[0])
# what is the number of dataset observations?
# count not just columns but populated rows.
