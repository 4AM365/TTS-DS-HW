# Exercise 1

# Guess a number between 1 to 9 given by the user

import random
print("Guess a number between 1-9. ")
target = random.randint(1, 9)
print("the actual number is ", target)
guess = 0
while(guess != target):
    guess = int(input("Enter your number: "))
print("Good guess")

# Exercise 2

# Check the validity of password input by the user.

# Validation:

#     At least 1 letter between [a-z] and 1 letter between [A-Z]
#     At least 1 number between [0-9]
#     At least 1 character from [$#@]
#     Minimum length 6 characters
#     Maximum length 16 characters

lower = 0
upper = 0
special = 0
number = 0
min = 0
max = 0
print("input password")
pw = input()
if (len(pw) >= 8):
    min = 1
if (len(pw) <= 16):
    mix = 1

for i in pw:
    if (i.islower()):
        lower = 1
        break
    elif (i.isupper()):
        upper = 1
        break
    if (i.isdigit()):
        number = 1
        break
    if(i == '$' or i == '#' or i == '@'):
        special = 1
        break
hsh = lower + upper + special + number + min + max
if (hsh == 6):
    print("password is acceptable")
else:
    print("password is insufficient")


# Exercise 3

# Get input of the age of 3 people by user and determine oldest and youngest among them

age1 = int(input("Enter age 1: "))
age2 = int(input("Enter age 2: "))
age3 = int(input("Enter age 3: "))
agelist = [age1, age2, age3]
print("the highest age is: ", max(agelist))
print("the lowest age is: ", min(agelist))


# Exercise 4

# A student will not be allowed to sit in exam if his/her attendance is less than 75%.

# Take following input from user

#     Number of classes held
#     Number of classes attended.
#     And print
#     percentage of class attended

classes = int(input("How many classes were held? "))
attended = int(input("How many classes were attended? "))
percent = float(100*attended/classes)
print("The student attended ", percent, "% of classes")
if(percent >= 75):
    print("The student may sit for the exam.")
elif(percent < 75):
    print("The student may not sit for the exam")

# Is student is allowed to sit in exam or not?

# Exercise 5

# Get an integer N from the user and perform the following actions:

#     if N is odd, print "weird"
#     if N is even and in the inclusive range of 2 to 5, print "Not Weird"
#     if N is even and in the inclusive range of 6 to 20, print "Weird"
#     if N is even and greater than 20, print "Not Weird"

N = int(input("Enter a number: "))
if(N % 2 != 0):
    print("weird")
if(N % 2 == 0 & 2 <= N <= 5):
    print("Not Weird")
if(N % 2 == 0 & 6 <= N <= 20):
    print("Weird")
if(N % 2 == 0 & N >= 20):
    print("Weird")
